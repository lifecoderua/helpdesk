


%h1 Listing tickets

%table
  %thead
    %tr
      %th
      %th Body
      %th Status
      %th Owner
      %th Updated

  %tbody
    - @tickets.each do |ticket|
      %tr
        %td= link_to ticket.subject, ticket
        %td= highlight(excerpt(ticket.body, params[:q][:subject_or_body_cont], radius: 50), params[:q][:subject_or_body_cont])
        %td= ticket.status
        %td= ticket.owner
        %td= I18n.l ticket.updated_at, format: :long

